function Main({ children }) {
  return (
    <main className="max-w-7xl mx-auto w-full px-8 max-[376px]:pt-36 pt-44 overflow-hidden transition-all duration-300">
      {children}
    </main>
  );
}

export default Main;
